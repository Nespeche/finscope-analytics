# Borrador de tareas v0.18 — superseded

> **Estado:** `HISTORICAL_EVIDENCE_NOT_ACTIVE`  
> **Origen:** `specs/001-fundamental-analysis-platform/tasks.md` de v0.18.  
> **Motivo:** fue generado con el gate de implementación abierto prematuramente, sin análisis post-tareas y sin el formato obligatorio de Spec Kit.  
> **Uso permitido:** trazabilidad histórica únicamente. No autoriza implementación ni debe reutilizarse como `tasks.md` activo.

---

# tasks.md — FinScope Analytics Fundamental-First Platform

**Autorización:** APPROVED_FOR_TASKS (auditoría v0.17, remediación v0.18)  
**tasksAuthorized:** true | **implementationAuthorized:** true  
**Fecha de generación:** 2026-07-21 | **Gate abierto:** 2026-07-21 (AUTH-027, Svelte 5)  
**Constitución:** 3.0.0

> **Estado:** Gate de implementación abierto. La implementación puede comenzar en secuencia de capas.

---

## Cómo leer este archivo

Las tareas están organizadas en **capas** de dependencia técnica. No se puede comenzar una capa si la anterior no está verificada. Cada tarea incluye:
- **Criterios de aceptación** referenciados (`AC-XXX`)
- **Fixtures de test** que definen "DONE"
- **Autoridad normativa** del dominio

---

## Capa 0 — Entorno y scaffolding (sin código de producto)

Estas tareas no producen lógica de negocio. Establecen el entorno de desarrollo.

### T-000 — Seleccionar framework UI `[DONE]`
- **Decisión:** Svelte 5 + Vite + Vitest + Playwright (ver `decisions/ui-framework.md`, AUTH-027)
- **Artefacto registrado:** `decisions/ui-framework.md`, `governance/authority-crosswalk.json` domainCount=27
- **Fecha de cierre:** 2026-07-21

### T-001 — Inicializar repositorio de implementación
- **Acción:** crear repo con estructura `src/`, `tests/`, `fixtures/` (que apunta a los fixtures del spec)
- **Restricción:** NO copiar schemas ni catálogos — los fixtures del spec son la fuente; el código los importa como `devDependency`
- **Done cuando:** `npm test` ejecuta sin código de producto y los linters pasan

### T-002 — Configurar aritmética decimal
- **Librería:** `decimal.js` v10+ (AUTH-026, `decisions/decimal-library.md`)
- **Acción:** crear módulo `src/core/decimal.js` que exporta `Decimal` con `precision=20, rounding=ROUND_HALF_EVEN`
- **Test:** verificar que `new Decimal("0.1").plus("0.2").toDecimalPlaces(12).toString()` === `"0.3"` (sin trailing zeros; el schema DecimalString prohíbe trailing zeros vía regex)
- **Done cuando:** no hay ningún uso de `Number` para aritmética en el módulo

---

## Capa 1 — Gateway SEC (adquisición de datos)

Depende de: Capa 0 completada.

### T-010 — Implementar cliente SEC Company Facts
- **Autoridad:** `contracts/sec-acquisition-policy.json` steps 0–3 (local cache → submissions → company_facts)
- **Budget:** máximo 14 llamadas externas por operación (FR-003)
- **User-Agent y contacto:** obligatorio por NFR-003 y `fairAccess` del policy
- **Fixtures de test:** `fixtures/sec/sec-acquisition-test-vectors.json` — los 4 vectores
  - SEC-ORDER-PRIMARY-COMPLETE: 2 llamadas, Company Facts resuelve todo
  - SEC-ORDER-SELECTIVE-FALLBACK: 4 llamadas, 2 conceptos fallan → 2 Company Concept
  - SEC-BUDGET-HARD-STOP: 14 llamadas exactas → partial result, no llamada 15
  - SEC-CACHE-ZERO-CALLS: 0 llamadas (cache hit)
- **AC:** AC-007 (budget), AC-008 (cache-first), AC-009 (fair access)
- **Done cuando:** los 4 fixtures pasan

### T-011 — Implementar cache local de datos SEC
- **Autoridad:** `contracts/browser-storage-contract.json`, `contracts/sec-acquisition.md`
- **Mecanismo:** IndexedDB (compartida con snapshots pero en object store separado)
- **Invariante:** cache lookup no cuenta como llamada externa (`cacheLookupCountsAsExternalCall: false`)
- **AC:** AC-010 (cache no incrementa contador externo)
- **Done cuando:** SEC-CACHE-ZERO-CALLS fixture pasa con 0 network requests

### T-012 — Implementar resolución de identidad (CIK / ticker)
- **Autoridad:** `data-model.md#issueridentity`, FR-001, FR-002
- **Flujo ambiguo:** múltiples CIK → local issue `identity_ambiguous` (nunca HTTP 422)
- **Fixture:** AC-001 (CIK canonical), AC-002 (ambigüedad)
- **Done cuando:** AC-001 y AC-002 pasan con el gateway real

---

## Capa 2 — Normalización XBRL

Depende de: Capa 1 (datos disponibles).

### T-020 — Implementar motor de mapeo XBRL
- **Autoridad:** `definitions/xbrl-mapping-catalog.json` v5.0.0
- **Reglas:**
  - Solo mappings con `status: ACTIVE` y `inferenceAllowed: false`
  - Selección de mapping: precedencia ascendente → `mappingId` ascendente
  - Fact states: `resolved | absent | ambiguous | incompatible`
- **AC:** AC-012 (exact primary), AC-013 (approved_alias lowest precedence), AC-014 (ambiguous → insufficient)
- **Done cuando:** los 3 AC pasan contra datos XBRL reales de prueba

### T-021 — Implementar política de deuda (FND_BORROWINGS_DEBT)
- **Autoridad:** `definitions/xbrl-mapping-catalog.json#debtPolicy` (borrowings-debt-v2)
- **Buckets activos:** shortTermBorrowings, currentPortionLongTermDebt, longTermDebtNoncurrent, commercialPaper
- **Bucket ausente documentado:** `otherApprovedInterestBearingBorrowings` → `known_absent_intent` (NF-002 cerrado)
- **Degradación:** si un solo bucket no resuelve → `partial`; si todos fallan → `insufficient`
- **Cascade:** insufficient borrowingsDebt → insufficient FND_NET_DEBT + FND_DEBT_EQUITY
- **Done cuando:** metric-test-vectors.json casos de FND_BORROWINGS_DEBT (nominal, insufficient_input, profile_not_allowlisted) pasan

### T-022 — Implementar cobertura agregada
- **Autoridad:** FR-010, `definitions/state-and-capability-catalog.json#coverageStates`
- **Estados:** `complete | partial | missing | not_applicable`
- **Regla:** cobertura no sustituye al estado del fact individual
- **AC:** AC-011 (cobertura de bundle completo)
- **Done cuando:** AC-011 pasa

---

## Capa 3 — Cálculo de métricas fundamentales

Depende de: Capa 2 (XBRL normalizado), T-002 (decimal.js configurado).

### T-030 — Motor de métricas fundamentales (24 métricas)
- **Autoridad:** `definitions/metric-catalog.json` v5.0.0
- **Librería:** `decimal.js` (AUTH-026)
- **Cada métrica implementa:** formulaId, inputIds, optionalInputIds, minimumQuality, roundingPolicy, profileAllowlist, periodPolicy
- **Formula especial:** FND_REVENUE → `identity` (V15-B03 cerrado; el input es `revenue` directamente)
- **Fixture de test:** `fixtures/metrics/metric-test-vectors.json` — 96 casos (32 × 3)
  - nominal (32): métrica disponible con datos suficientes
  - insufficient_input (32): datos insuficientes → estado `insufficient`
  - profile_not_allowlisted (32): perfil no permitido → estado `not_applicable`
- **AC:** AC-031–AC-040 (cálculo de métricas)
- **Done cuando:** los 96 fixtures pasan con aritmética `decimal.js`

### T-031 — Motor de calidad fundamental
- **Autoridad:** `definitions/quality-model-catalog.json#fundamental`
- **Ejes:** mappingQuality, completeness, comparability, revisionState (cardinality 144)
- **Función total:** siempre produce una clasificación (el `otherwise` cubre todos los casos)
- **Regla:** no score numérico, solo ordinal
- **AC:** AC-014 (mappingQuality ambiguous → insufficient)
- **Done cuando:** todas las combinaciones de ejes dan clasificación sin excepción

---

## Capa 4 — Motor de reglas (insights)

Depende de: Capa 3 (métricas disponibles).

### T-040 — Implementar evaluador de AST de reglas
- **Autoridad:** `definitions/insight-rule-catalog.json` v5.0.0, `schemas/rule-node.schema.json`
- **Operadores:** all, any, count_at_least, comparison, metric_state_is, quality_is_at_least
- **Regla:** una regla no se evalúa si un input requerido está indisponible o bajo la calidad mínima → `not_evaluable`
- **Fixture de test:** `fixtures/rules/rule-test-vectors.json` — 27 casos (9 × 3: triggered, not_triggered, not_evaluable)
- **AC:** AC-051–AC-060 (evaluación de insights)
- **Done cuando:** los 27 fixtures pasan

---

## Capa 5 — Fingerprints y evidencia

Depende de: Capas 3–4 (análisis completo).

### T-050 — Implementar JCS + SHA-256 fingerprinting
- **Autoridad:** `contracts/fingerprint-projections.json` v5.0.0, FR-025–FR-029
- **Algoritmo:** RFC 8785 JCS (claves ordenadas, sin whitespace) + SHA-256 con prefijo `sha256:`
- **Proyecciones:** 5 fingerprints con campos excluidos explícitos
- **Fixture de test:** `contracts/fingerprint-test-vectors.json` — 4 vectores positivos + 3 invarianza + 8 negativos
- **Invariante clave:** campos de reloj local NUNCA entran en fingerprint (V15-B01, V15-B02 cerrados)
- **Done cuando:** los 15 vectores pasan

### T-051 — Implementar sourceEvidenceFingerprint
- **Proyección:** `contracts/fingerprint-projections.json#sourceEvidenceFingerprint`
- **Cobertura:** todos los facts XBRL usados como input, con sus metadatos (no el resultado calculado)
- **Done cuando:** el fingerprint es determinista ante mismos inputs y cambia ante cualquier modificación de facts

---

## Capa 6 — Persistencia local (IndexedDB)

Depende de: Capa 5 (snapshot completo con fingerprints).

### T-060 — Implementar persistencia atómica IndexedDB
- **Autoridad:** `contracts/browser-storage-contract.json`, FR-034–FR-036
- **Mecanismo:** compare-and-swap de pointer en una transacción atómica
- **Fixture de test:** `fixtures/storage/indexeddb-atomicity-test-vectors.json` — 6 vectores
  - IDB-FUNDAMENTAL-COMMIT: commit exitoso
  - IDB-FUNDAMENTAL-ROLLBACK-BEFORE-POINTER: rollback antes de actualizar pointer
  - IDB-PRICE-COMMIT-INDEPENDENT: commit de precio independiente del fundamental
  - IDB-PRICE-ROLLBACK: rollback de precio
  - IDB-PRICE-DELETE: borrado de precio
  - IDB-DELETE-ALL-ROLLBACK: rollback de borrado total
- **AC:** AC-073–AC-075 (atomicidad)
- **Done cuando:** los 6 fixtures pasan incluyendo los escenarios de fallo

### T-061 — Implementar consentimiento de persistencia
- **Autoridad:** FR-030–FR-032, `definitions/local-operation-issue-catalog.json#storage_consent_required`
- **Flujo:** si no hay consentimiento → `refreshConsent=false` → renderizar sin red
- **Done cuando:** AC-076 (consentimiento explícito antes de escribir) pasa

---

## Capa 7 — Pipeline de estados y orquestador

Depende de: Capas 1–6 (todos los componentes disponibles).

### T-070 — Implementar máquina de estados del pipeline
- **Autoridad:** `definitions/state-and-capability-catalog.json#pipelineStates`, `fixtures/states/pipeline-transition-cartesian.json`
- **Estados:** idle, checking, acquiring, normalizing, analyzing, ready, partial, failed, cancelled (9 estados)
- **Transiciones:** solo las 81 listadas en el Cartesian están permitidas; todo lo demás es prohibido
- **Transiciones clave:** ready→idle y partial→idle son permitted=true (V15-B11 cerrado)
- **Fixture de test:** `fixtures/states/pipeline-transition-cartesian.json` — 81 entradas
- **Done cuando:** todas las transiciones no listadas son rechazadas y las 81 listadas funcionan correctamente

### T-071 — Implementar Web Worker del orquestador
- **Autoridad:** `contracts/analysis-pipeline.md`, `contracts/update-orchestration.md`
- **Responsabilidades:** controlar estado, cancelación, reanudación, secuencia de adquisición, publicación atómica
- **Invariante:** el orquestador no usa reloj local para fingerprints
- **AC:** AC-041–AC-050 (pipeline states y recovery)
- **Done cuando:** el pipeline completo pasa de idle → ready en un happy path y maneja cancelled correctamente

---

## Capa 8 — Gateway API y Problem Details

Depende de: Capa 7 (orquestador operativo).

### T-080 — Implementar gateway con Problem Details RFC 9457
- **Autoridad:** `contracts/openapi.yaml`, `schemas/problem-details.schema.json`
- **11 endpoints** (todos GET), sin 409 ni 422
- **10 variantes Problem Details:** cada una con `status.const`, `type.const` URI, `additionalProperties: false`
- **Fixture:** `definitions/gateway-problem-details-catalog.json#operationMatrix`
- **AC:** AC-061–AC-070 (Problem Details)
- **Done cuando:** todos los errores del gateway producen `application/problem+json` con la variante correcta

---

## Capa 9 — Overlay de precio histórico

Depende de: Capa 6 (IndexedDB). Independiente de capas 7–8 para lógica fundamental.

### T-090 — Implementar importación CSV de precio
- **Autoridad:** `contracts/historical-price-import.md`
- **Validación:** `schemas/historical-price-overlay.schema.json`
- **Sin reloj local:** sin `displayAgeDays`, `evaluationDate`, `asOfLocalDate` en el overlay (V15-B01 cerrado)
- **Fixture:** `fixtures/price/historical-price-overlay-test-vectors.json` — 5 fixtures
- **Done cuando:** PRICE-OVERLAY-VALID pasa y PRICE-OVERLAY-LOCAL-CLOCK-FORBIDDEN es rechazado por schema

### T-091 — Implementar 8 métricas de precio
- **Autoridad:** `definitions/metric-catalog.json` (class: market_descriptive)
- **Calidad:** modelo `historicalPrice` — 4 ejes (adjustmentDisclosure, currencyIntegrity, dateIntegrity, rowValidity), cardinality 54
- **Invariante:** métricas de precio nunca intervienen en reglas fundamentales (FR-023)
- **Done cuando:** los 8 × 3 = 24 metric fixtures de clase `market_descriptive` pasan

---

## Capa 10 — UI / Accesibilidad

Depende de: Capas 7–9 (pipeline completo y datos disponibles).

### T-100 — Implementar estados de UI con accesibilidad WCAG 2.2 AA
- **Autoridad:** FR-041, FR-042, NFR-007
- **AC accesibilidad:** AC-071 (aria-busy/disabled — NF-001 cerrado con oracle determinista)
  - aria-busy=true durante checking|acquiring|normalizing|analyzing
  - aria-disabled=true cuando failed|cancelled
  - ambos ausentes en idle|ready|partial
- **AC:** AC-071–AC-084 (accesibilidad y estados UI)
- **Done cuando:** AC-071 pasa con el oracle exacto

### T-101 — Implementar visualización de evidencia y fingerprints
- **Autoridad:** FR-041, `contracts/fingerprint-and-sanitization.md`
- **Requisito:** evidencia, fingerprints, estados y recuperación visibles al usuario
- **AC:** AC-081–AC-084
- **Done cuando:** el usuario puede ver el hash de fingerprint y trazar qué facts lo componen

---

## Capa 11 — Integración y smoke test end-to-end

Depende de: Todas las capas anteriores.

### T-110 — Happy path con empresa real
- **Emisor de prueba:** Apple Inc. (CIK 0000320193) — empresa US-GAAP estándar
- **Verificar:** pipeline idle→ready con 1 llamada Company Facts + 0 Company Concept
- **Verificar:** 24 métricas calculadas, todas en estado `available` o `partial`
- **Verificar:** fingerprint reproducible (correr 2 veces con mismos datos → mismo hash)

### T-111 — Smoke test unsupported_profile
- **Emisor de prueba:** banco o aseguradora fuera de allowlist
- **Verificar:** perfil detectado como `unsupported_profile` → identidad y filings visibles, métricas `not_applicable`, sin error HTTP

### T-112 — Smoke test budget agotado (SEC-BUDGET-HARD-STOP)
- **Verificar:** con un emisor con muchos conceptos no-resueltos, el pipeline publica `partial` al alcanzar 14 llamadas y no ejecuta la llamada 15

---

## Orden de implementación sugerido

```
Capa 0 (entorno) → Capa 1 (SEC gateway) → Capa 2 (XBRL) →
Capa 3 (métricas) → Capa 4 (reglas) → Capa 5 (fingerprints) →
Capa 6 (IndexedDB) → Capa 7 (pipeline) → Capa 8 (API gateway) →
Capa 9 (precio) → Capa 10 (UI) → Capa 11 (integración)
```

Las capas 8 y 9 pueden desarrollarse en paralelo con la capa 7 una vez que las capas 1–6 están estables.

---

## Criterios globales de "DONE" del proyecto

- Todos los fixtures de test del spec pasan: 96 (métricas) + 27 (reglas) + 81 (pipeline) + 15 (fingerprints) + 6 (IndexedDB) + 5 (overlay) + 4 (SEC) = **234 fixtures**
- Todos los AC relevantes verificados: AC-001 a AC-084
- No hay usos de `Number` para aritmética financiera en código de producción
- No hay 409 ni 422 emitidos por el gateway
- No hay campos de reloj local en ningún fingerprint
- El pipeline completo (idle→ready) funciona en menos de 14 llamadas SEC para empresas estándar

---

*Este archivo fue generado bajo autorización APPROVED_FOR_TASKS del checklist v0.17, con remediaciones v0.18 aplicadas. La implementación de cualquier tarea requiere aprobación explícita del gate de implementación.*

