# Actualización del registro arquitectónico — v0.10

Se agregaron ADR-013 a ADR-023 en `reports/ARCHITECTURE_DECISION_LOG.md`:

- frontera raw + contrato canónico único;
- MIC requerido/nullable;
- valuación híbrida conservadora;
- confianza híbrida `[0,100]`;
- estados discriminados y Problem Details;
- fallback SEC selectivo, presupuesto 10 y `dei` acotado;
- política efectiva determinística;
- catálogo único de aceptación.

No se modifica ninguna decisión de plataforma. No se instala ni selecciona una dependencia nueva. Las decisiones sustituyen contradicciones documentales previas y conservan los informes v0.9 como evidencia histórica.
