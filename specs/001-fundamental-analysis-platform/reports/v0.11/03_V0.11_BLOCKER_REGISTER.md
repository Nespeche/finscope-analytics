# Registro de bloqueantes — v0.11

**Gate:** `NO APROBADO PARA TAREAS`  
**Bloqueantes abiertos:** 7

| ID | Bloqueo | Evidencia | Por qué impide tareas | Cierre requerido | Documentos afectados |
|---|---|---|---|---|---|
| V11-B01 | El fingerprint de mercado depende de una proyección raw no normalizada normativamente | `contracts/canonical-market-contract.md:141,179-183` incluye `payloadFingerprint` en la proyección canónica, pero lo define como hash de un “payload raw sanitizado” solo “cuando sea legal conservarlo”; `contracts/provider-adapter.md:55` ordena sanitizar sin fijar proyección, serialización ni regla de null. | Dos adaptadores conformes pueden sanear o serializar el mismo payload semántico de forma distinta, o usar null según una evaluación no definida, y producir fingerprints diferentes con los mismos datos canónicos. Se rompe la reproducibilidad exigida y no pueden definirse tareas determinísticas. | Definir una única proyección raw sanitizada y su serialización, el criterio exhaustivo para valor/null, o excluir `payloadFingerprint` del fingerprint de `MarketSeries` y conservarlo solo como evidencia no determinante. | canonical-market-contract.md; provider-adapter.md; openapi.yaml; analysis-pipeline.md; browser-storage.md; acceptance-criteria-catalog.md |
| V11-B02 | La frescura de mercado basada en sesiones no es computable cuando calendario o zona son desconocidos | `spec.md:110` prohíbe inferir calendario y zona; `spec.md:156` clasifica precio por 1, 2–5 o más de 5 sesiones; `canonical-market-contract.md:155-163` permite `marketCalendar=unknown` y `timeZone=unknown` mientras exige `freshnessState`. | Con los mismos datos y fecha de evaluación, implementaciones distintas pueden contar sesiones de forma diferente o no poder clasificarlas. Cambian gates de valuación, caps y resultados de confianza. | Definir fecha/hora de evaluación, calendario normativo por identidad o un resultado terminal obligatorio cuando calendario/zona sean unknown; especificar fines de semana, feriados, zona y límites inclusivos. | spec.md; canonical-market-contract.md; state-and-capability-matrix.md; valuation-compatibility-policy.md; insight-rule-catalog.md; metric-catalog.md |
| V11-B03 | ValuationContext no discrimina de forma única la base de acciones ni las razones de compatibilidad | `valuation-compatibility-policy.md:33-39` mezcla en `SharesBasis` método de obtención, completitud multiclase y conversión depositaria; `:61` exige `valuationCompatibilityReason[]` sin vocabulario, tipo, orden ni correspondencia; `:85` admite ADR como `compatible` o `compatible_with_limitations` sin predicado exhaustivo. `metric-catalog.md:763,797,830...` usa descripciones narrativas en lugar de los valores del enum. | Una misma evidencia puede serializarse con diferentes `SharesBasis`, razones y estados, o perder simultáneamente la derivación y la completitud. Las tareas tendrían que inventar un modelo discriminado y reglas de decisión; los ratios pueden parecer válidos con contextos no comparables. | Separar ejes ortogonales o definir un objeto discriminado completo; cerrar códigos/orden de razones y predicados de cada estado, incluido ADR; exigir valores canónicos exactos en las nueve métricas. | valuation-compatibility-policy.md; data-model.md; metric-catalog.md; analysis-pipeline.md; insight-rule-catalog.md; acceptance-criteria-catalog.md; GLOSSARY.md |
| V11-B04 | La función de confianza y síntesis no es total y contiene una contradicción neutral/insufficient_information | `insight-rule-catalog.md:69-73` define la capa neutral con el mínimo de métricas elegibles sin umbral; `:81-82` exige score >=40 para neutral y score <40 para insufficient. Con métricas elegibles 50 y 30 y ninguna regla activa, ambas salidas aplican. `:99` y AC-020–028 usan `confidenceScore`, mientras `data-model.md:93-96` define `score`/`rawScore`. Además, completeness, temporalComparability y “context score” no tienen predicados exhaustivos. | Los mismos inputs y versiones pueden producir resultados distintos; no es posible generar tareas ni fixtures con un oráculo único. La contradicción afecta todas las reglas y la síntesis final. | Unificar el nombre canónico; definir elegibilidad, campos requeridos, categorías de comparabilidad, score de contexto y precedencia; resolver explícitamente el caso de métricas elegibles con mínimo <40 y todos los empates/gates. | insight-rule-catalog.md; data-model.md; analysis-pipeline.md; acceptance-criteria-catalog.md; quickstart.md; reglas INS-001–INS-012 |
| V11-B05 | Problem Details y la matriz de errores no constituyen contratos discriminados por ErrorCode | `state-and-capability-matrix.md:99,103-105` usa `partial o unavailable` sin predicado para varios códigos; `openapi.yaml:822-908` modela un `ProblemDetails` genérico con `additionalProperties: true`, sin `oneOf`/const por código; las respuestas `:1187-1240` reutilizan ese esquema y no fijan status, operationState, coverage, metric state, recovery o retryability. La matriz contempla 409 para cancelación (`state...:107`), pero los paths no publican 409. | El contrato permite combinaciones imposibles y no garantiza la matriz `ErrorCode → HTTP → estados → recuperación → accesibilidad`. Cliente, Worker y tests podrían implementar semánticas incompatibles. | Definir variantes discriminadas por `code` con valores const y predicados exhaustivos para partial/unavailable; separar provider_unavailable de invalid_payload si corresponde; publicar cancelación y validar capacidades/mensajes por variante. | state-and-capability-matrix.md; contracts/openapi.yaml; provider-adapter.md; analysis-pipeline.md; spec.md; acceptance-criteria-catalog.md |
| V11-B06 | El fallback SEC no distingue concepto ausente de CIK inexistente y contradice el conteo de llamadas cacheadas | `xbrl-mapping-catalog.md:11-17` establece que cache hits cuestan 0 y submissions cuesta 0–1 según “metadata fresca” no definida. `openapi.yaml:186-219` responde 404 de Company Concept con `CikNotFound`, sin resultado `concept_absent` que permita continuar a alias. `openapi.yaml:918-938,1098-1149` impone `callsUsed >=1`, incompatible con respuestas cacheadas de costo 0 y sin aclarar si el valor es por respuesta o acumulado. | No existe transición contractual determinística desde tag primario ausente al alias, y el presupuesto disponible puede variar según una definición de frescura/cache no cerrada. Los mismos inputs pueden terminar en complete, partial o unavailable distintos. | Modelar ausencia de concepto separada de `cik_not_found`; definir TTL/revalidación y condición de submissions; especificar `callsUsed` acumulado y permitir 0 cuando no hubo upstream; cerrar attemptedConcept result para absent/error/cancelled. | xbrl-mapping-catalog.md; openapi.yaml; analysis-pipeline.md; data-model.md; state-and-capability-matrix.md; acceptance-criteria-catalog.md |
| V11-B07 | No existe construcción canónica de totalDebt ni derivación normativa completa de EBITDA | `xbrl-mapping-catalog.md:79,85,88-89` enumera operating income, D&A y deuda corriente/no corriente, pero no define `EBITDA = operatingIncome + D&A` ni una precedencia directa/derivada, y solo ordena evitar solapamientos de deuda sin algoritmo de `totalDebt`. `metric-catalog.md:561-640,784-1012` consume `totalDebt` y `ebitdaTTM` como inputs canónicos. | Seis métricas y la regla INS-002 pueden variar materialmente, duplicar deuda corriente o usar EBITDA no equivalente. Existe riesgo directo de ratios financieros engañosos. | Definir concepto canónico/algoritmo de totalDebt con tags mutuamente excluyentes y precedencia; definir EBITDA directo permitido o fórmula derivada, períodos, signo, aliases, trazabilidad, confianza, estados y criterios de aceptación. | xbrl-mapping-catalog.md; metric-catalog.md; valuation-compatibility-policy.md; analysis-pipeline.md; data-model.md; insight-rule-catalog.md; acceptance-criteria-catalog.md |

Mientras uno permanezca abierto, `tasks.md` está prohibido.
