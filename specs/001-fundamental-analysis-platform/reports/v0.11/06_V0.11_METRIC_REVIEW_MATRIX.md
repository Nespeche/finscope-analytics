# Matriz de revisión de las 36 métricas — v0.11

| Métrica | Fórmula | Unidad | Cero | Null | Revisión local | Hallazgo/dependencia |
|---|---|---|---|---|---|---|
| MKT_LAST_CLOSE | `close[n]` | precio | close>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MIN_CLOSE | `min(close_i)` | precio | no permitido; todo `close` canónico es >0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MAX_CLOSE | `max(close_i)` | precio | no permitido; todo `close` canónico es >0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_AVG_CLOSE | `sum(close_i)/n` | precio | denominador n>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MEDIAN_CLOSE | `median(close_i)` | precio | no permitido; todo `close` canónico es >0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_RANGE_POSITION | `(last-min)/(max-min)` | ratio | si max=min -> not_meaningful | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_SIMPLE_RETURN | `last/first-1` | porcentaje | first>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_VOLATILITY_ANN | `stdev_s(ln(close_t/close_t-1))*sqrt(252)` | porcentaje anual | requiere >=20 retornos y ajuste compatible | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MAX_DRAWDOWN | `min(close_t/runningMax_t-1)` | porcentaje | runningMax>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MA20 | `sum(last20)/20` | precio | n>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MA50 | `sum(last50)/50` | precio | n>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_MA200 | `sum(last200)/200` | precio | n>0 | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| MKT_TREND_DESC | `up si last>MA20 y MA20>MA20[-5]; down si last<MA20 y MA20<MA20[-5]; otherwise sideways` | enum | ajuste compatible | unavailable; nunca convertir a cero. | CONFORME | Definición local consistente; reproducibilidad/frescura de la serie bloqueadas (`V11-B01`, `V11-B02`). |
| FND_REVENUE_GROWTH | `revenue_t/revenue_prev-1` | porcentaje | revenue_prev>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_GROSS_MARGIN | `grossProfit/revenue` | porcentaje | revenue>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_OPERATING_MARGIN | `operatingIncome/revenue` | porcentaje | revenue>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_NET_MARGIN | `netIncome/revenue` | porcentaje | revenue>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_ROA | `netIncomeTTM/avg(totalAssets_begin,totalAssets_end)` | porcentaje | activos promedio>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_ROE | `netIncomeTTM/avg(equity_begin,equity_end)` | porcentaje | patrimonio promedio>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_CURRENT_RATIO | `currentAssets/currentLiabilities` | ratio | currentLiabilities>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_DEBT_EQUITY | `totalDebt/equity` | ratio | equity>0 | unavailable; nunca convertir a cero. | BLOQUEADA | `totalDebt` carece de construcción canónica (`V11-B07`). |
| FND_NET_DEBT | `totalDebt-cashAndEquivalents` | moneda | componentes compatibles | unavailable; nunca convertir a cero. | BLOQUEADA | `totalDebt` carece de construcción canónica (`V11-B07`). |
| FND_NET_DEBT_EBITDA | `netDebt/ebitdaTTM` | ratio | ebitdaTTM>0 | unavailable; nunca convertir a cero. | BLOQUEADA | `totalDebt` y `ebitdaTTM` carecen de derivación canónica (`V11-B07`). |
| FND_CFO | `CFO` | moneda | fact válido | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_CAPEX | `abs(paymentsToAcquirePPE)` | moneda | fact válido | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_FCF | `CFO-CAPEX` | moneda | componentes compatibles | unavailable; nunca convertir a cero. | CONFORME | — |
| FND_CASH_CONVERSION | `CFOTTM/netIncomeTTM` | ratio | netIncomeTTM>0 | unavailable; nunca convertir a cero. | CONFORME | — |
| VAL_MARKET_CAP | `Σ(pricePerUnderlyingShare_c × sharesOutstandingExTreasury_c)` para todas las clases económicas requeridas; en clase única, un solo término. | moneda. | precio>0 y shares>0. | `unavailable` o `blocked`; nunca convertir a cero. | CONFORME | Definición local consistente; ejecución bloqueada por ValuationContext (`V11-B03`). |
| VAL_ENTERPRISE_VALUE | `issuerTotalMarketCap + totalDebt - cashAndEquivalents`. | moneda. | componentes cero permitidos si informados y compatibles. | `unavailable`; nunca convertir a cero. | BLOQUEADA | EV consume `totalDebt` no reconciliado (`V11-B07`) y ValuationContext ambiguo (`V11-B03`). |
| VAL_PE | `issuerTotalMarketCap / netIncomeTTMAttributable`. | múltiplo. | denominador debe ser >0. | `unavailable`/`blocked`; nunca cero. | CONFORME | Definición local consistente; ejecución bloqueada por ValuationContext (`V11-B03`). |
| VAL_EARNINGS_YIELD | `netIncomeTTMAttributable / issuerTotalMarketCap`. | porcentaje. | market cap >0; numerador cero permitido. | `unavailable`/`blocked`; nunca cero por ausencia. | CONFORME | Definición local consistente; ejecución bloqueada por ValuationContext (`V11-B03`). |
| VAL_PS | `issuerTotalMarketCap / revenueTTMConsolidated`. | múltiplo. | revenue debe ser >0. | `unavailable`/`blocked`. | CONFORME | Definición local consistente; ejecución bloqueada por ValuationContext (`V11-B03`). |
| VAL_PB | `issuerTotalMarketCap / equityAttributableToParent`. | múltiplo. | equity debe ser >0. | NCI no separable/material → `unavailable`; otras incompatibilidades → `blocked`. | CONFORME | Definición local consistente; ejecución bloqueada por ValuationContext (`V11-B03`). |
| VAL_EV_SALES | `issuerEnterpriseValue / revenueTTMConsolidated`. | múltiplo. | revenue debe ser >0. | `unavailable`/`blocked`. | BLOQUEADA | Hereda EV no determinístico (`V11-B07`) y ValuationContext ambiguo (`V11-B03`). |
| VAL_EV_EBITDA | `issuerEnterpriseValue / ebitdaTTMConsolidated`. | múltiplo. | EBITDA debe ser >0. | `unavailable`/`blocked`. | BLOQUEADA | Hereda EV y EBITDA no determinísticos (`V11-B07`) y ValuationContext ambiguo (`V11-B03`). |
| VAL_FCF_YIELD | `freeCashFlowTTMConsolidated / issuerTotalMarketCap`. | porcentaje. | market cap >0; FCF cero permitido. | `unavailable`/`blocked`; nunca cero por ausencia. | CONFORME | Definición local consistente; ejecución bloqueada por ValuationContext (`V11-B03`). |

## Resumen

- Revisadas: 36/36.
- Sin contradicción intrínseca de fórmula/metadatos: 30.
- Bloqueadas directamente por inputs canónicos faltantes: 6 (`FND_DEBT_EQUITY`, `FND_NET_DEBT`, `FND_NET_DEBT_EBITDA`, `VAL_ENTERPRISE_VALUE`, `VAL_EV_SALES`, `VAL_EV_EBITDA`).
- Las 13 métricas de mercado heredan `V11-B01`/`V11-B02`.
- Las 9 métricas de valuación heredan `V11-B03`; tres también `V11-B07`.
- Revalidación V09-I02: **cerrada** para mínimo, máximo, mediana, deuda neta y FCF.
