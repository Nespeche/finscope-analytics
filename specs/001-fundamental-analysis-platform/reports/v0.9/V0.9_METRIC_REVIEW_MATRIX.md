# Matriz de revisión de las 36 métricas — ejecución v0.9

Cada fila fue revisada por fórmula, numerador/denominador, signo, unidad, moneda, período, ventana, redondeo, muestra, cero, negativos, nulos, sector, ajuste, identidad, alineación temporal, confianza y limitaciones.

| Métrica | Fórmula | Resultado conceptual | Observación |
|---|---|---|---|
| `MKT_LAST_CLOSE` | `close[n]` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_MIN_CLOSE` | `min(close_i)` | `FAIL — important` | Fórmula principal interpretable, pero metadatos contradictorios (`V09-I02`). |
| `MKT_MAX_CLOSE` | `max(close_i)` | `FAIL — important` | Fórmula principal interpretable, pero metadatos contradictorios (`V09-I02`). |
| `MKT_AVG_CLOSE` | `sum(close_i)/n` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_MEDIAN_CLOSE` | `median(close_i)` | `FAIL — important` | Fórmula principal interpretable, pero metadatos contradictorios (`V09-I02`). |
| `MKT_RANGE_POSITION` | `(last-min)/(max-min)` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_SIMPLE_RETURN` | `last/first-1` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_VOLATILITY_ANN` | `stdev_s(ln(close_t/close_t-1))*sqrt(252)` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_MAX_DRAWDOWN` | `min(close_t/runningMax_t-1)` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_MA20` | `sum(last20)/20` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_MA50` | `sum(last50)/50` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_MA200` | `sum(last200)/200` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `MKT_TREND_DESC` | `up si last>MA20 y MA20>MA20[-5]; down si last<MA20 y MA20<MA20[-5]; otherwise sideways` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_REVENUE_GROWTH` | `revenue_t/revenue_prev-1` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_GROSS_MARGIN` | `grossProfit/revenue` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_OPERATING_MARGIN` | `operatingIncome/revenue` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_NET_MARGIN` | `netIncome/revenue` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_ROA` | `netIncomeTTM/avg(totalAssets_begin,totalAssets_end)` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_ROE` | `netIncomeTTM/avg(equity_begin,equity_end)` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_CURRENT_RATIO` | `currentAssets/currentLiabilities` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_DEBT_EQUITY` | `totalDebt/equity` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_NET_DEBT` | `totalDebt-cashAndEquivalents` | `FAIL — important` | Fórmula principal interpretable, pero metadatos contradictorios (`V09-I02`). |
| `FND_NET_DEBT_EBITDA` | `netDebt/ebitdaTTM` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_CFO` | `CFO` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_CAPEX` | `abs(paymentsToAcquirePPE)` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `FND_FCF` | `CFO-CAPEX` | `FAIL — important` | Fórmula principal interpretable, pero metadatos contradictorios (`V09-I02`). |
| `FND_CASH_CONVERSION` | `CFOTTM/netIncomeTTM` | `PASS` | Definición suficientemente explícita dentro de su aplicabilidad y bloqueos documentados. |
| `VAL_MARKET_CAP` | `price*compatibleShares` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_ENTERPRISE_VALUE` | `marketCap+totalDebt-cash` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_PE` | `marketCap/netIncomeTTM` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_EARNINGS_YIELD` | `netIncomeTTM/marketCap` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_PS` | `marketCap/revenueTTM` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_PB` | `marketCap/equity` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_EV_SALES` | `EV/revenueTTM` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_EV_EBITDA` | `EV/ebitdaTTM` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |
| `VAL_FCF_YIELD` | `FCFTTM/marketCap` | `FAIL — blocking` | Fórmula explícita, pero depende de unidad de cuenta de market cap/shares no cerrada para multiclase/ADR (`V09-B02`). |

**Control de alcance:** `dividend yield` y `total return` permanecen fuera del MVP.
