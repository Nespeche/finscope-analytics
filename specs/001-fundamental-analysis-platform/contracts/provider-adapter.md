# Provider adapter contract

**Estado:** `DEFERRED_POST_MVP`.

IDs reservados: `twelve_data_provider_v1` y `alpha_vantage_provider_v1`. Ningún adapter está habilitado. El único perfil activo de precio es `local_csv_manual_v1`. No se permiten credenciales ni llamadas de proveedor en el MVP.
